spring:
  datasource:
    url: jdbc:mysql://localhost:3306/EMPLOYEERECURRINGPLANS
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database: mysql

server:
  port: 8082
